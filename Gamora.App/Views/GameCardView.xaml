<UserControl x:Class="Gamora.App.Views.GameCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             Width="170" Margin="14">

    <!-- CardRoot: açılış animasyonunun hedefi (fade + yukarıdan kayma) -->
    <Grid x:Name="CardRoot" Opacity="0">
        <Grid.RenderTransform>
            <TranslateTransform x:Name="EntranceTranslate" Y="16" />
        </Grid.RenderTransform>

        <Button x:Name="RootButton"
                Command="{Binding SelectCommand}"
                Background="Transparent"
                BorderThickness="0"
                Padding="0"
                Cursor="Hand"
                HorizontalContentAlignment="Stretch">
            <Button.RenderTransform>
                <ScaleTransform x:Name="HoverScale" ScaleX="1" ScaleY="1" />
            </Button.RenderTransform>
            <Button.RenderTransformOrigin>0.5,0.5</Button.RenderTransformOrigin>
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <ContentPresenter />
                </ControlTemplate>
            </Button.Template>
            <Button.Style>
                <Style TargetType="Button">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.05" Duration="0:0:0.18">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.05" Duration="0:0:0.18">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.15" />
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>

            <Border x:Name="AccentBorder"
                    Background="{StaticResource CardBackgroundBrush}"
                    BorderThickness="2">
                <Border.BorderBrush>
                    <SolidColorBrush Color="Transparent" />
                </Border.BorderBrush>
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMouseOver, ElementName=RootButton}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="BorderBrush.Color" To="{StaticResource AccentColor}" Duration="0:0:0.18" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                                <DataTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="BorderBrush.Color" To="Transparent" Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.ExitActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="255" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" ClipToBounds="True">
                        <Image Source="{Binding CoverImage}"
                               Stretch="UniformToFill"
                               Visibility="{Binding CoverImage, Converter={StaticResource NullToVisibilityConverter}}" />

                        <Border Background="{StaticResource CardBackgroundBrush}"
                                BorderBrush="{StaticResource AccentBrush}"
                                BorderThickness="0,2,0,0"
                                Visibility="{Binding CoverImage, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}">
                            <TextBlock Text="{Binding Initials}"
                                       FontFamily="{StaticResource RajdhaniFont}"
                                       FontSize="48"
                                       Foreground="{StaticResource PlaceholderTextBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>

                        <!-- Popüler rozeti: en çok başlatılan ilk 10 oyunda görünür -->
                        <Border Width="20" Height="20"
                                CornerRadius="10"
                                Background="#B30E1015"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Margin="0,6,6,0"
                                Visibility="{Binding IsPopular, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ui:SymbolIcon Symbol="Fire16"
                                           FontSize="14"
                                           Foreground="{StaticResource AccentBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center" />
                        </Border>
                    </Grid>

                    <StackPanel x:Name="NameStrip" Grid.Row="1" Margin="8,8,8,10">
                        <StackPanel.RenderTransform>
                            <TranslateTransform x:Name="NameShift" Y="0" />
                        </StackPanel.RenderTransform>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMouseOver, ElementName=RootButton}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y" To="-6" Duration="0:0:0.18">
                                                        <DoubleAnimation.EasingFunction>
                                                            <QuadraticEase EasingMode="EaseOut" />
                                                        </DoubleAnimation.EasingFunction>
                                                    </DoubleAnimation>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Y" To="0" Duration="0:0:0.15" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <TextBlock Text="{Binding Name}"
                                   FontFamily="{StaticResource RajdhaniFont}"
                                   FontSize="16"
                                   TextTrimming="CharacterEllipsis"
                                   Foreground="White" />
                        <TextBlock Text="{Binding Category}"
                                   FontSize="11"
                                   Margin="0,2,0,0"
                                   TextTrimming="CharacterEllipsis"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </StackPanel>

                    <!-- Başlatma kilidi: aynı karta arka arkaya tıklamayı görsel olarak da caydırır -->
                    <Border Grid.Row="0" Grid.RowSpan="2"
                            Background="#B30E1015"
                            Visibility="{Binding IsLaunching, Converter={StaticResource BoolToVisibilityConverter}}" />
                </Grid>
            </Border>
        </Button>
    </Grid>
</UserControl>
