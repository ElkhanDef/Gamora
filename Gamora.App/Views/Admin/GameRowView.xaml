<UserControl x:Class="Gamora.App.Views.Admin.GameRowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:Gamora.App.ViewModels.Admin"
             d:DataContext="{d:DesignInstance Type=vm:GameRowViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="RowIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RowBg" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RowBg" Property="Background" Value="{StaticResource CardBackgroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24,0,24,0" MinHeight="60">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="140" />
            <ColumnDefinition Width="130" />
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="76" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Width="40" Height="56" VerticalAlignment="Center" Background="{StaticResource CardBackgroundBrush}">
            <Grid>
                <Image Source="{Binding CoverImage}" Stretch="UniformToFill" Visibility="{Binding CoverImage, Converter={StaticResource NullToVisibilityConverter}}" />
                <TextBlock Text="{Binding Initials}"
                           FontFamily="{StaticResource RajdhaniFont}"
                           FontSize="13"
                           Foreground="{StaticResource PlaceholderTextBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding CoverImage, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}" />
            </Grid>
        </Border>

        <TextBlock Grid.Column="1"
                   Text="{Binding Name}"
                   VerticalAlignment="Center"
                   Margin="12,0,8,0"
                   FontSize="14"
                   Foreground="White"
                   TextTrimming="CharacterEllipsis" />

        <TextBlock Grid.Column="2"
                   Text="{Binding Category}"
                   VerticalAlignment="Center"
                   Margin="0,0,8,0"
                   FontSize="12"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   TextTrimming="CharacterEllipsis" />

        <TextBlock Grid.Column="3"
                   Text="{Binding LaunchTypeLabel}"
                   VerticalAlignment="Center"
                   Margin="0,0,8,0"
                   FontSize="12"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   TextTrimming="CharacterEllipsis" />

        <TextBlock Grid.Column="4" VerticalAlignment="Center" FontSize="12" FontWeight="Medium">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Text" Value="Görünür" />
                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Visible}" Value="False">
                            <Setter Property="Text" Value="Gizli" />
                            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <TextBlock Grid.Column="5"
                   Text="{Binding SortOrder}"
                   VerticalAlignment="Center"
                   FontSize="12"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <StackPanel Grid.Column="6" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
            <Button Style="{StaticResource RowIconButtonStyle}"
                    Command="{Binding DataContext.EditGameCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                    CommandParameter="{Binding}"
                    ToolTip="Düzenle">
                <ui:SymbolIcon Symbol="Edit24" FontSize="16" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </Button>
            <Button Style="{StaticResource RowIconButtonStyle}"
                    Margin="4,0,0,0"
                    Command="{Binding DataContext.DeleteGameCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                    CommandParameter="{Binding}"
                    ToolTip="Sil">
                <ui:SymbolIcon Symbol="Delete24" FontSize="16" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </Button>
        </StackPanel>

        <Border Grid.ColumnSpan="7" Height="1" VerticalAlignment="Bottom" Margin="0,0,0,-1" Background="{StaticResource CardBackgroundBrush}" />
    </Grid>
</UserControl>
